DATA1 SEGMENT
VB DB 101B
СРІМЇ DB 'ПРИВЄТ'
DATA1 ENDS

DATA2 SEGMENT
VW DW 407D
VD DD 56FDAH
DATA2 ENDS

CODE SEGMENT
ORG 100H
START :
STI

PUSH ES
POP AX
PUSH BX
POP CX

POINT :

TEST DI, SI
TEST AX, DX
TEST BL, CL

OR BL, FS: VB[DI]
JZ POINT

SUB AX[SI], BX

ROR AH, 01B
ROR BX, 10
ROR CX, A1H

JZ EXIT

SHR VB[SI], CL

OR AX, VD[SI]

EXIT :
CODE ENDS

END _START

CODE ENDS